<UserControl x:Class="Storager.Misc.LoadingScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Storager.Misc"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Rectangle x:Name="LoadingBarRectangle"
                   Height="50">
            <Rectangle.Fill>
                <LinearGradientBrush x:Name="BorderLGB">
                    <GradientStop x:Name="GradientStop3" Color="Wheat" Offset="0"/>
                    <GradientStop x:Name="GradientStop1" Color="Green" Offset="1"/>
                    <GradientStop x:Name="GradientStop2" Color="Wheat" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
    </Grid>
</UserControl>
